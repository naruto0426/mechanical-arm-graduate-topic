demo-python program is in darknet folder, it need to install https://github.com/AlexeyAB/darknet first
calibration data is in servo_data
the program that predict the point that cannot be catched is in plot_no_go